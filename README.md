# Terser comparator for JavaScript minification

To run the Terser comparator, use the following commands:

```bash
npm install
node index.mjs myfile.nocache.js
```

The differences will be printed to report.txt.